{% set name = "qgis-plugin-manager" %}
{% set version = "1.6.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/qgis-plugin-manager-{{ version }}.tar.gz
  sha256: 91493f6a56638e7c02f7de6f0bc4450bd5647af39cd981a0c1ade864be371d28

build:
  entry_points:
    - qgis-plugin-manager = qgis_plugin_manager.__main__:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.7
    - pip
  run:
    - python >=3.7

test:
  imports:
    - qgis_plugin_manager
  commands:
    - pip check
    - qgis-plugin-manager --help
  requires:
    - pip

about:
  home: https://github.com/3liz/qgis-plugin-manager
  summary: Tool for downloading/managing QGIS plugins from CLI.
  dev_url: https://github.com/3liz/qgis-plugin-manager
  license: 'GPL-3.0-only'
  license_file: LICENSE

extra:
  recipe-maintainers:
    - yuvipanda
